% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lfs_microdata.R
\name{lfs_microdata}
\alias{lfs_microdata}
\title{Load LFS microdata}
\usage{
lfs_microdata(
  start_date,
  end_date,
  filter_condition = NULL,
  filter_months = NULL,
  filter_years = NULL,
  add_custom_dvs = TRUE,
  weight_var = "FINALWT",
  prerelease = FALSE,
  bootstraps = FALSE,
  tabs_plus = FALSE,
  supplement = FALSE,
  supplement_lmi = FALSE,
  supplement_lmsi = FALSE,
  supplement_dlmi = FALSE,
  north = FALSE,
  add_labels = FALSE,
  language = "EN",
  ...
)
}
\arguments{
\item{start_date}{Start date of the range (YYYY-MM-DD).}

\item{end_date}{End date of the range (YYYY-MM-DD).}

\item{filter_condition}{Optional filter condition expression (e.g., "AGE >= 15").}

\item{filter_months}{Optional months to filter (numeric vector or comma-separated string).}

\item{filter_years}{Optional years to filter (character vector or comma-separated string).}

\item{add_custom_dvs}{Whether to add custom derived variables (default: TRUE).}

\item{weight_var}{Weighting variable to use (default: "FINALWT").}

\item{prerelease}{Whether to use pre-release data (default: FALSE).}

\item{bootstraps}{Whether to include bootstrap weights (default: FALSE).}

\item{tabs_plus}{Whether to include TABS Plus data (default: FALSE).}

\item{supplement_lmi}{Whether to include LMI (monthly) supplement data (default: FALSE).}

\item{supplement_lmsi}{Whether to include LMSI (quarterly) supplement data (default: FALSE).}

\item{supplement_dlmi}{Whether to include DLMI (annual disability) supplement data (default: FALSE).}

\item{north}{Whether to include north data (default: FALSE).}

\item{add_labels}{Whether to apply labels (default: FALSE).}

\item{language}{Language for labels, "EN" or "FR" (default: "EN").}

\item{...}{Additional parameters passed to the processing functions.}
}
\value{
A data frame containing LFS microdata records.
}
\description{
Retrieves Labour Force Survey (LFS) microdata for the specified date range without
any aggregation or summary calculations. This function provides direct access to
individual records for custom analysis.
}
\details{
This function retrieves individual LFS survey records without performing any
aggregation or summary calculations. It's useful for custom analysis or when
you need to work with raw microdata.

Unlike lfs_table(), this function does not require or use the estimates parameter
and does not perform any summary calculations. It simply loads and filters the
data according to the specified parameters.
}
\examples{
\dontrun{
# Basic usage: Get all records for one month
microdata <- lfs_microdata(
  start_date = "2025-01-01",
  end_date = "2025-01-01"
)

# Filtered example: Only employed people age 25-54
employed_core <- lfs_microdata(
  start_date = "2025-01-01",
  end_date = "2025-03-01",
  filter_condition = "AGE >= 25 & AGE <= 54 & EMPLOYED == 1",
  add_labels = TRUE
)
}

}
