% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lfs_table.R
\name{lfs_table}
\alias{lfs_table}
\title{Create LFS summary tables with flexible estimate types}
\usage{
lfs_table(
  start_date,
  end_date,
  estimates = NULL,
  analysis_vars = "PROV",
  filter_condition = "AGE >= 15 & !is.na(LFSSTAT)",
  weight_var = "FINALWT",
  moving_avg = 1,
  filter_months = NULL,
  filter_years = NULL,
  add_custom_dvs = TRUE,
  prerelease = FALSE,
  bootstraps = FALSE,
  tabs_plus = FALSE,
  supplement = FALSE,
  supplement_lmi = FALSE,
  supplement_lmsi = FALSE,
  supplement_dlmi = FALSE,
  north = FALSE,
  add_labels = TRUE,
  language = "EN",
  weight_rounding = 100,
  include_marginals = TRUE,
  add_suppression_flag = FALSE,
  calculate_change = FALSE,
  lag_period = 1,
  calculate_difference = FALSE,
  comparison_variable = NULL,
  comparison_categories = NULL,
  ...
)
}
\arguments{
\item{start_date}{Start date of the range (YYYY-MM-DD).}

\item{end_date}{End date of the range (YYYY-MM-DD).}

\item{estimates}{List of estimate configurations. This can be either:
- A nested list of estimate configurations (for multiple estimates)
- A single list with est_type (for a single estimate)
- Omitted (defaults to a simple sum)}

\item{analysis_vars}{Variables to analyze (comma-separated string or character vector).
Default is "PROV".}

\item{filter_condition}{Global filter condition applied during data loading (e.g., "AGE >= 15").
This filter is applied before any estimate-specific filters.}

\item{weight_var}{Weighting variable to use (default: "FINALWT").}

\item{moving_avg}{Number of periods for moving average (default: 1).}

\item{filter_months}{Optional months to filter (numeric vector or comma-separated string).}

\item{filter_years}{Optional years to filter (character vector or comma-separated string).}

\item{add_custom_dvs}{Whether to add custom derived variables (default: TRUE).}

\item{prerelease}{Whether to use pre-release data (default: FALSE).}

\item{bootstraps}{Whether to include bootstrap weights (default: FALSE).}

\item{tabs_plus}{Whether to include TABS Plus data (default: FALSE).}

\item{supplement_lmi}{Whether to include LMI (monthly) supplement data (default: FALSE).}

\item{supplement_lmsi}{Whether to include LMSI (quarterly) supplement data (default: FALSE).}

\item{supplement_dlmi}{Whether to include DLMI (annual disability) supplement data (default: FALSE).}

\item{north}{Whether to include north data (default: FALSE).}

\item{add_labels}{Whether to apply labels (default: FALSE).}

\item{language}{Language for labels, "EN" or "FR" (default: "EN").}

\item{...}{Additional parameters passed to the processing functions.}
}
\value{
A data frame containing LFS tabular results for all estimates.
}
\description{
Create LFS summary tables with flexible estimate types
}
