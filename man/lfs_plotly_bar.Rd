% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lfs_visualizations.R
\name{lfs_plotly_bar}
\alias{lfs_plotly_bar}
\title{Create a formatted bar chart using Plotly}
\usage{
lfs_plotly_bar(
  data,
  x,
  y,
  color = NULL,
  title = "Chart ",
  subtitle = "",
  source = "",
  y_range = NULL,
  y_title = "",
  colors = c("#234399", "#B52049", "grey50", "#8FAADC"),
  width = 800,
  height = 600
)
}
\arguments{
\item{data}{A data frame containing the variables to plot}

\item{x}{Character string specifying the column name for the x-axis}

\item{y}{Character string specifying the column name for the y-axis}

\item{color}{Optional character string specifying the column name for color grouping.
If NULL, all bars will be colored with the first color from the \code{colors} palette.}

\item{title}{Chart title. Default is "Chart "}

\item{subtitle}{Optional subtitle text. Default is empty.}

\item{source}{Source information text. If empty (""), the source annotation will not be included.}

\item{y_range}{Optional numeric vector of length 2 specifying y-axis range. If NULL (default),
Plotly's automatic ranging is used.}

\item{y_title}{Optional y-axis title. Default is empty.}

\item{colors}{Color palette for bars. Default is c("#234399", "#B52049", "grey50", "#8FAADC").}

\item{width}{Chart width in pixels. Default is 800.}

\item{height}{Chart height in pixels. Default is 600.}
}
\value{
A plotly object
}
\description{
This function creates a standardized bar chart with Plotly, designed with consistent
styling and appropriate spacing based on the elements included. It can handle both
single and multi-series data.
}
\details{
The function automatically adjusts spacing and layout based on whether subtitle and color
grouping are used. It applies standardized formatting for titles, annotations, and legend
positioning. When \code{color} is not specified, all bars will adopt the first color from the
\code{colors} vector.
}
\examples{
\dontrun{
# Basic usage with single line
lfs_plotly_bar(
  data = unemployment_data,
  x = "DATE",
  y = "RATE",
  title = "Unemployment Rate"
)

# With color grouping and subtitle
lfs_plotly_bar(
  data = unemployment_data,
  x = "DATE",
  y = "RATE",
  color = "GENDER",
  title = "Unemployment Rate by Gender",
  subtitle = "Monthly rates, seasonally adjusted"
)
}

}
